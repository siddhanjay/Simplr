import { ILogEntry, IRPCWaitForLogsRequest, QtumRPC } from "./QtumRPC";
export declare class LogsSubscriber {
    private _rpc;
    private _waitReq;
    private _emitter;
    constructor(_rpc: QtumRPC, _waitReq?: IRPCWaitForLogsRequest);
    /**
     * async loop to long-poll for new logs
     */
    start(): Promise<void>;
    on(fn: (log: ILogEntry) => void): void;
    once(fn: (log: ILogEntry) => void): void;
    off(fn: (log: ILogEntry) => void): void;
}
